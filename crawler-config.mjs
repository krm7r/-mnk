// ============================================================
// Ultimate Web Scraper - Remote Worker Configuration
//
// عدّل هذا الملف فقط لتشغيل الـCrawler عن بُعد.
// لا تحتاج لتعديل worker/index.mjs في الاستخدام العادي.
// ============================================================

export const CONFIG = {
  // ----------------------------------------------------------
  // [1] عدد المواقع في التشغيل الواحد
  // الحد الأقصى الحالي: 10 مواقع.
  // مثال: 2 يعني تشغيل الموقع الأول والثاني في نفس الـRun.
  // ----------------------------------------------------------
  maxSites: 2,

  // ----------------------------------------------------------
  // [2] أقصى عدد صفحات يتم استخراجها من كل موقع
  // الحد الأقصى الحالي: 5000 صفحة لكل موقع.
  // ----------------------------------------------------------
  maxPagesPerSite: 5000,

  // ----------------------------------------------------------
  // [3] أقصى عدد صفحات Chromium تعمل في نفس اللحظة.
  // أنت طلبت ألا يزيد العدد عن 10.
  // هذا الحد إجمالي لكل المواقع معًا، وليس 10 لكل موقع.
  // ----------------------------------------------------------
  concurrency: 10,

  // ----------------------------------------------------------
  // [4] وقت انتظار بعد فتح الصفحة بالمللي ثانية.
  // 2500 = 2.5 ثانية.
  // قلله للمواقع السريعة، وزده للمواقع الثقيلة/الديناميكية.
  // ----------------------------------------------------------
  waitMs: 2500,

  // ----------------------------------------------------------
  // [5] تنزيل ملفات CSS و JavaScript المرتبطة بالصفحات.
  // true = نعم / false = لا
  // ----------------------------------------------------------
  downloadAssets: true,

  // ----------------------------------------------------------
  // [6] إعدادات الموقع الأول
  // ضع رابط الموقع هنا.
  // include = روابط/مسارات تريد السماح بها فقط (اختياري).
  // exclude = كلمات/مسارات تريد استبعادها (اختياري).
  // ----------------------------------------------------------
  sites: [
    {
      // ===== عدّل رابط الموقع الأول هنا =====
      url: 'https://example.com',

      // ===== أقصى صفحات لهذا الموقع (اختياري) =====
      maxPages: 5000,

      // ===== عدد صفحات المتصفح لهذا الموقع (السقف العام ما زال 10) =====
      concurrency: 10,

      // ===== اتركها [] لاستخراج الموقع كله =====
      include: [],

      // ===== مثال: ['login', 'admin', 'cart'] =====
      exclude: [],

      // ===== تنزيل CSS/JS لهذا الموقع =====
      downloadAssets: true,
    },

    // --------------------------------------------------------
    // [7] الموقع الثاني
    // احذف أو عطّل هذا الجزء إذا كنت تريد موقعًا واحدًا فقط.
    // --------------------------------------------------------
    {
      // ===== عدّل رابط الموقع الثاني هنا =====
      url: 'https://example.org',

      maxPages: 5000,
      concurrency: 10,
      include: [],
      exclude: [],
      downloadAssets: true,
    },
  ],

  // ----------------------------------------------------------
  // [8] إعداد رفع النتيجة إلى Google Drive
  //
  // لا تضع كلمة مرور أو Access Token حقيقي داخل هذا الملف.
  // الأفضل وضع رابط سكربت الرفع في GitHub Secret.
  //
  // مثال للمتغير الذي ستضعه لاحقًا في GitHub:
  // DRIVE_UPLOAD_URL = https://script.google.com/macros/s/XXXXX/exec
  // ----------------------------------------------------------
  drive: {
    enabled: false,
    // ===== ضع رابط Web App الخاص برفع Google Drive هنا لاحقًا =====
    uploadUrl: '',
  },
};
